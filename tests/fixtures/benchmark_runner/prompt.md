# Synthetic runner conformance fixture

Write the exact candidate token `correct` followed by one newline.

This fixture validates AXIOM-Bench runner mechanics only. It is not a language
benchmark task and must never appear in AXIOM-Bench suite results.
