profile system;

fn increment(value: &mut i32) -> i32 {
    *value = *value + 1;
    return *value;
}

fn main() -> i32 {
    var value: i32 = 41;
    if true {
        let shared: &i32 = &value;
        if *shared != 41 {
            return 0;
        }
    }
    return increment(&mut value);
}
