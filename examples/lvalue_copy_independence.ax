profile system;

struct Pair {
    left: i32,
    right: i32,
}

fn main() -> i32 {
    var first: Pair = Pair { left: 1, right: 2 };
    let second: Pair = first;
    first.left = 20;
    return first.left + second.left + second.right;
}
